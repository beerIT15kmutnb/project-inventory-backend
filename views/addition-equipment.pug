include css.pug
doctype html
html
  head
    meta(charset="utf-8")
    title report
  body
    button.btn.btn-sm.btn-success(onclick='window.print()') Print
  
    table.table
      thead
        tr 
          td.center.bold(colspan='4') ประมาณการวัสดุสำนักงาน ปราจีนบุรี งบ #{year}	
      tbody
        tr.head.center
          td.center(width="10%") ลำดับที่ 
          td(width="60%") รายการ (ลักษณะและขนาด) 
          td(colspan='2' width="30%").center จำนวนที่เบิก
        - var i=0
        - for(var page of _rs)
          - i++
          tr
            td.center #{i}
            td.left.bold(colspan='3') #{page.equipment_name}
          - for(var detail of page.detail)
            tr
              td.center 
              td.left #{detail.product_name}
              td.right.border-right( width="15%") #{detail.req_qty} 
              td.border-left(style="border-right:0" width="15%") #{detail.unit_name}
        .pagebreak